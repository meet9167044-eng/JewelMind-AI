[pytest]
# Run tests from the project root so backend.app.* imports resolve correctly.
testpaths = backend/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
